# intern-program
Added notes to the README file and made a commit 

Set user name: git connfig --global user.name "Gyudo-Nam"

Set user email: git config --global user.email"namgyudo49@gmail.com"

git init: Creates a git repository in the current folder

git clone (URL): Copies the contents of a remote repository to your computer

git status: Checks the status of which files have been modified

git add (file name): Adds modified files to the staging area 

git add.: Adds all changed files at once

git commit -m "message": Permanently records prepared files in the repository 

git push origin (branch name): Uploads your work from your computer to GitHub 

git pull origin(branch name): Fetches the latest content from GitHub and merges it to your computer

git remote -v: Checks the currently connected remote repository 